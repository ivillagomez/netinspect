# Direct dependencies — pinned to exact versions for reproducible builds.
# To update: bump versions here, rebuild, re-test, then commit.
# Run `pip install -r requirements.txt` to install.
fastapi==0.136.0
uvicorn[standard]==0.46.0
netmiko==4.6.0
httpx==0.28.1
pyyaml==6.0.3
pydantic==2.12.5
python-multipart==0.0.26
puresnmp==2.0.1
aiohttp>=3.9.0          # optional: Cisco RESTCONF + Aruba CX REST API
